import React from 'react'

import type {Metadata} from "next";

export const metadata: Metadata = {
  title: "Pacyfic login page",
  description: "login to shopping",
};

const page = () => {
  return (
    <div>
        
    </div>
  )
}

export default page