import React from 'react'

import type {Metadata} from "next";

export const metadata: Metadata = {
  title: "Unishop login page",
  description: "login to shopping",
};

const page = () => {
  return (
    <div>
        
    </div>
  )
}

export default page